# PRD — 이음이 (어르신 디지털·스마트폰 안내 챗봇)

## 도메인
어르신 디지털·스마트폰 이용 안내 (공개 문서 근거 기반 RAG 챗봇)

## 문제 정의
어르신과 그 가족은 디지털배움터 신청 방법, 보이스피싱·스미싱 대처, 무인민원발급기·정부24 이용처럼 **공공 제도에 이미 답이 나와 있는 질문**을 어디서 확인해야 할지 몰라 헤맵니다. 일반 챗봇은 그럴듯하게 답하지만 근거를 제시하지 않아, 잘못된 정보가 사기 피해나 헛걸음으로 이어질 위험이 있습니다.

이 챗봇은 **공개 문서 15건 안에서만** 답하고, 답변마다 근거 ID와 원문 링크를 함께 보여 줍니다. 자료에 없으면 지어내지 않고 없다고 말합니다.

## 타겟 유저

| 구분 | 주 사용자 | 보조 사용자 |
|---|---|---|
| 누구 | 60세 이상 디지털 교육·지원 제도를 알아보는 어르신 | 어르신을 대신해 알아보는 가족·복지관 담당자 |
| 상황 | 스마트폰 사용이 서툴고 글자가 작으면 읽기 어려움. 모르는 용어가 나오면 중단함 | 정확한 신청처·전화번호·비용을 빠르게 확인하고 전달해야 함 |
| 필요 | 큰 글씨, 쉬운 말, 한 화면에 하나의 초점, 말로 묻기 | 출처 원문 확인, 링크 전달 |
| 하지 않는 일 | 회원가입, 로그인, 개인정보 입력 | 신청 대행 |

## 목표
- 60세 이상 이용자와 그 가족이 공공 디지털 교육·지원 제도를 공개 문서 근거와 함께 확인한다.
- 모든 답변에 출처 [ID]와 원문 URL을 함께 표시한다.
- 근거 문서에 전화번호·오프라인 창구가 명시돼 있으면 URL과 함께 안내한다.
- 자료에 없는 질문은 넓혀 답하지 않고, 범위 밖임을 밝힌 뒤 안내 가능한 주제를 알려 준다.

## 비목표
- 메신저(LINE/텔레그램) 연동은 하지 않는다.
- 통신 요금제 비교·기기 구매 추천은 하지 않는다(범위 밖 + 부드러운 대안 안내로 처리).
- 개인정보 입력·로그인·서버 저장은 하지 않는다. 신청 대행은 하지 않는다(안내만).
- 의료·법률·금융 상담은 하지 않는다.
- 다국어 지원, 서버 측 대화 기록 수집은 이번 범위에 포함하지 않는다.

## 핵심 기능 (MVP)

기능 목록이 아니라 **사용자의 판단을 돕는 이유**를 함께 적습니다.

| 기능 | 하는 일 | 사용자의 판단을 돕는 이유 |
|---|---|---|
| 하이브리드 근거 검색 | 코사인 top-10 + BM25 top-5 + RRF 융합 | 표현이 달라도, 정확한 낱말이어도 근거를 찾게 해 "답이 없어서"와 "못 찾아서"를 구분 |
| 근거 표시 답변 생성 | 로컬 Ollama 스트리밍, 문장 뒤 `[ID]` | 생성 문장이 자연스럽다는 사실과 출처가 있다는 사실을 구분해 보여 줌 |
| 출처 칩 + 원문 모달 | ID·섹션·검색 방법·유사도 표시, 클릭 시 원문 조각 | 답변을 그대로 믿지 않고 원문까지 되짚을 경로를 제공 |
| 약한 근거 경고 | 최고 유사도 < 0.33이면 경고 배지 + 조심스러운 말투 | 확신의 정도를 화면에 드러내 과신을 막음 |
| 범위 밖 거절 | 최고 유사도 < 0.30이면 LLM 호출 없이 거절 | 지어낸 답을 원천 차단 |
| 자동 판정(judge) | 6필드 JSON 판정을 배지·점수로 표시 | 답변 품질을 인상이 아니라 관찰 가능한 값으로 남김 |
| 사람 피드백 | 도움됐어요 / 아쉬워요 | 자동 판정과 사람 판단이 어긋나는 지점을 찾는 재료 |
| 생성 중지 | 스트리밍 취소 | 오래 걸리거나 엉뚱한 답이 시작될 때 사용자가 끊을 권리 |
| 연결 상태 배너 | Ollama 연결·모델 확인, 실패 시 OS별 복구 안내와 재시도 | 페이지 오류와 자기 실행 환경 문제를 구분하게 함 |
| 접근성 | 큰 글씨·고대비 토글, 음성 입력, 답변 읽어주기 | 시력·타이핑이 어려운 사용자가 중단 없이 끝까지 도달 |

## 화면 구성

단일 페이지(플랫 구조)이며, 위에서 아래로 한 흐름입니다. 메뉴 계층을 만들지 않습니다.

| 영역 | 내용 | 이유 |
|---|---|---|
| 히어로 | 서비스 이름·한 줄 소개·챗봇으로 이동 | 처음 보는 사람이 무엇을 묻는 곳인지 먼저 판단 |
| 안내 3카드 | 서비스 소개 / 근거 원칙 / 실행 구조 | 실행 전에 사용 조건(로컬 Ollama 필요)을 스스로 판단 |
| 챗봇 패널 머리말 | 자료 건수·모델명·연결 상태 칩 | 지금 무엇으로 답하는지 노출 |
| 연결 안내 배너 | 미연결 시에만 노출. 브라우저·모델·CORS 3단계 + 다시 확인 | 실패해도 사용자가 사라진 상태를 겪지 않게 함 |
| 접근성 툴바 | 큰 글씨 / 고대비 | 첫 화면에서 바로 노출(학습 불필요) |
| 자주 묻는 질문 | 카드 5개 | 입력하지 않고 한 번에 시작 |
| 대화 영역 | 질문 표시 → 약한 근거 경고 → 답변 → 듣기 → 출처 칩 → 피드백·자동판정 → 판정 배지 | 근거 → 답변 → 판정 순서를 화면 순서로 고정 |
| 근거 원문 모달 | 원문 조각·코사인/BM25 점수·원문 링크 | 출처 칩이 장식이 아니라 검증 경로가 되게 함 |
| 입력 영역 | 마이크 · 입력창 · 질문하기(생성 중에는 중지) | 항상 화면 하단에 고정 |

## 근거 자료 계획

- 수집 범위: 디지털배움터(과기정통부·NIA 운영) 공개 페이지, 정책브리핑 보도자료, 지자체 어르신 디지털 지원 안내, 보이스피싱·스미싱 예방 및 무인민원발급기·정부24 이용 안내.
- 청크 규칙: 최소 120자 이상의 독립된 사실 단위. 한 청크에 서로 다른 주제를 섞지 않는다.
- 스키마: `id`(SD-000 연속 부여) · `text` · `url`(실제로 연 공개 주소만) · `section` · `vector`(768차원).
- 현재 15건(SD-001~SD-015). 원본은 `senior-docs.source.json`, 임베딩 포함본은 `senior-docs.json`.
- 갱신 책임: 제도·전화번호·URL이 바뀌면 원본 JSON을 고치고 `embed-docs.mjs`로 재임베딩한 뒤 30문항 재검증을 통과시킨다. 기존 ID가 가리키는 원문은 바꾸지 않는다.

## 수용 기준 — 자동 판정 (LLM-as-a-Judge 6필드)
- 모든 답변에 [ID] 근거 표시 + 원문 URL 병기 → cited
- 도메인 밖 질문은 refusal:true (거절 사유는 밝히되 어조는 부드럽게) → refusal
- 대안 안내에 문서에 없는 기관명·전화번호·URL을 새로 만들지 않는다 → noHalluc
- 답변 내용이 검색 근거 안에 있어야 한다 → grounded
- 종합 품질 점수 → score(0~100)
- 판정 코멘트(한국어) → comment
- refusal:true면 grounded/noHalluc/cited는 판단 대상에서 제외한다(화면에서도 "제외"로 표시).

## 수용 기준 — 수동 점검 (사람이 눈으로 확인, judge 6필드 밖)
- 최고 유사도 < 0.33(THRESHOLD)일 때 단정 표현 없이 조심스러운 어조로 전환하는가. (BM25 강매칭 BM25_MIN>=6.0이면 어조 전환 없음 — 원안 0.55는 모델 교체 후 코사인 유사도 분포 변화로 실측 조정됨)
- 전 답변이 한국어 존댓말이며, 어려운 용어를 풀어 설명하는가.
- 출처 칩을 눌러 원문 조각이 답변 내용과 실제로 맞는가.
- Ollama가 꺼진 상태에서 배너와 재시도가 보이는가.

## 수용 기준 — 배포 후 점검
- 배포 주소에서 자산(정적 JSON·이미지)이 정상 로드되는가.
- OLLAMA_ORIGINS 설정 후 배포 주소에서 답변 스트리밍이 되는가.
- 대표 질문 5개(FAQ) 중 근거 있는 답변이 4개 이상인가.
- 도메인 밖 질문 1개 이상에서 거절 안내가 표시되는가.

## 실행 조건
- 답변 모델: qwen3.5:2b 고정(Ollama 로컬), think:false, stream:true, temperature 0.3.
- 임베딩: Ollama 로컬 API(embeddinggemma, 768차원) — 임베딩 계산만 로컬 위임, 검색 계산은 브라우저에서 수행.
- 검색: 코사인 top-10 + BM25 top-5 + RRF(k=60) 하이브리드. THRESHOLD=0.33(weak), REFUSE=0.30(거절), BM25_MIN=6.0(강매칭 시 weak/refuse 해제).
- 임베딩 준비율: 사전임베딩(정적 JSON) 방식이라 실시간 다운로드 진행률(준비율) 개념 없음.
- 브라우저: Chrome·Edge 권장. Safari는 https 페이지에서 로컬 Ollama 호출이 차단됨.

## 설계 메모 — 목표↔비목표 충돌 지점
- 경계 미끄러짐(교육→기기): 친절하려다 범위를 넘을 위험 → 수용기준 refusal/noHalluc으로 방어.
- 전화번호 안내 ↔ 개인정보 비목표: 안내만, 신청 대행 없음.
- 가독성(용어 풀이) ↔ 정확성([ID]+URL): 답변 본문에는 [ID]를 남기고, 어려운 말은 풀어 쓰는 방식으로 조율.

## 설계 메모 — 검증 가능한 질문 유형
자격/대상, 신청 방법, 비용, 운영 장소·주체, 배우는 내용, 지원 제도.

## 설계 메모 — 질문 예시
- 도메인 밖: 오늘 날씨 / 손주 선물 추천 / 주식 추천
- 근거 부족: 우리 경로당 다음 교육 날짜 / ○○구 이번 달 정원
- 애매: 디지털 교육 좋아요? / 그거 어때요?

## 설계 메모 — 추가 검토 제안
- 상충 근거: 지역별로 다르면 각 출처를 함께 제시.
- 시점: 문서에 연도가 있으면 함께 표기.
- 부분 근거: 답 가능한 부분만 근거와 함께, 나머지는 확인 불가 명시.

## 사용자 피드백 기능
- 각 답변에 사람 피드백(도움됐어요/아쉬워요)을 남길 수 있는 UI를 제공한다.
- 사람 피드백은 자동 판정(judge) 결과와 함께 화면에서 확인할 수 있어야 한다.
- 생성 문장이 자연스럽다는 사실과 출처를 가진 답이라는 사실은 다르므로, 출처 칩과 경고 배지로 두 사실을 구분해 표시한다.
- (발전) 축적된 피드백으로 실패 사례를 모아 프롬프트·검색 세팅 개선에 활용한다.
