# MiMo 인수인계 — UI CSS 개선 작업 (2026-08-29)

이 문서는 opencode 데스크탑 화면에서 **MiMo(V2.5 Free) 모델로 진행할 UI 개선 작업** 인수인계서다.
직전 big-pickle 세션이 아래 내용을 모두 조사·확인 후 남긴 것이다. 이 문서만 읽고 바로 작업을 시작하면 된다.

---

## 1. 작업 대상 (UI 개선, 외형만 — 기능 비변경)

**현재 남은 작업은 UI CSS 개선 하나다.** 그 외 judge 이중화 실험(커밋 `1688d3e`)은 완료됐고,
README 수정은 마커스님이 직접 하신다(건드리지 말 것).

상태: **아직 어떤 UI 파일도 수정하지 않았다.** 전부 인라인 스타일로 구현돼 있고, Vite 데모 CSS가 잔존한다.

### 수정할 파일 3개 (app/ 아래)
| 파일 | 현재 상태 |
|---|---|
| `app/src/App.tsx` | 모든 스타일이 인라인 `style={{...}}` (108줄). 배지·카드·판정박스 색상 하드코딩 |
| `app/src/index.css` | Vite 데모 스타일(light/dark 변수 포함) + **미사용 `.counter`, `.hero`, `#center`, `#next-steps`, `#docs`, `.ticks` 잔존** |
| `app/src/App.css` | **Vite 데모용**(`.counter`, `.hero`, `#spacer`, `#next-steps`만 존재). 실제 앱 클래스 전무 |

### 현재 화면 구성 (App.tsx)
헤더(h1) → 상태 배지 3개 → 입력줄(입력+질문+중지) → weak 경고 → 답변 영역 → 출처 칩(하이퍼링크) → 피드백(👍/👎) + 자동판정 버튼 → 판정 박스(점수+6필드+comment)

---

## 2. 개선 지침 (마커스님이 사전에 승인한 설계안)

1. **기능·로직 비변경** — 검색·스트리밍·judge·피드백 로직은 그대로. 순수 CSS/클래스만.
2. **Vite 데모 CSS 제거** — `index.css`의 미사용 클래스·`App.css` 전체를 실제 앱용으로 교체
3. **색상 CSS 변수화** — `index.css`의 `:root`에 토큰 정의(배경·텍스트·카드·경계·accent), 현재 하드코딩된
   `#dcfce7`(배지 green)·`#fee2e2`(배지 red)·`#fafafa`(판정박스)·`#b45309`(weak 경고) 등을 변수로 흡수
4. **light/dark 대응** — `index.css`에 이미 `prefers-color-scheme: dark` 블록이 있으니 변수 세트를 dark용으로도 제공
5. **카드형 레이아웃** — 답변 영역·판정 박스: 경계선+배경+둥근 모서리(카드), 배지·출처 칩은 색상·간격 정리
6. **어르신 대상** — 기본 글자 크기 16→17~18px, 줄간격 1.7 유지, 버튼 크기 살짝 확대
7. **색상 계열** — 전문적이면서 따뜻한 청록/파랑 계열을 제안(마커스님이 다른 의견 내시면 그걸 우선)

---

## 3. 작업 절차 (지시서 원칙 — 필수 준수)

1. **단계별 승인**: 한 번에 전체 수정 금지. 각 단계 시작 전 **①무엇 ②현재 상태 ③예상 결과 ④다음 단계**를 보고하고
   마커스님 승인 후 진행한다. 마커스님은 "수동 확인 모드"를 요구한다.
2. **파일 작성**: 큰 파일은 python3 스크립트(read-modify-write, idempotent)로. **덮어쓰기 전 백업**하고 백업 경로 보고.
3. **오류 처리**: 오류/결과 불량 시 그 상태에서 재시도 금지. 생성물 즉시 삭제 → 확인 → 재작업.
4. **검증**: `cd app && npm run dev` → http://localhost:5173 에서 화면을 **마커스님이 직접 확인**한다.
   (이전 모델은 이미지를 볼 수 없어 스크린샷 확인이 불가했음 — MiMo도 이미지를 본다면 활용)
5. **커밋**: 의미 있는 단위마다 커밋. README·HANDOFF는 마커스님 몫이니 커밋하지 말 것.

---

## 4. 잊지 말 것

- **이모지 금지** (마커스님 전역 규칙) — 응답·파일에 이모지 쓰지 않기
- **한국어로 응답**, 마커스님을 "마커스님"으로 호칭
- Ollama 실행 중(`ollama serve`), dev 서버 동작 확인됨
- README.md·HANDOFF.md 수정은 **하지 않는다** (마커스님이 직접 수정 예정)
- 저장소 커밋은 로컬 4개 ahead 상태. push는 마커스님이 지시할 때만.

---

## 5. 참고 파일 (읽으면 맥락 완성)
- `~/다운로드/OPENCODE지시서.md` — 전체 작업 지침 (특히 섹션 0~5, 7-5 UI 항목)
- `PRD.md` / `README.md` — 프로젝트 목적·아키텍처
- `app/src/rag.ts` — 현재 사용 중인 인덱싱·검색·chatStream·judge (건드리지 않음)